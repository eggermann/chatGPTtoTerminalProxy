# Changelog

## Unreleased

- Added cwd/path-aware `fresh`, so `parasit.sh fresh /path/to/project` can target a folder or a file path directly.
- Added optional `prompt-concator` watcher support for generated `input-prompt.txt` project context.
- Added VS Code autosave settings for project workspaces that use this bridge.
- Updated the startup prompt and docs so `commands.txt` stays the final shell-command sink.
- Very experimental and stack dependent, but just for fun.

